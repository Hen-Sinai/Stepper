package roles;

public class ReadOnlyFlowsRole extends RoleImpl {
    public ReadOnlyFlowsRole() {
        super("Read Only Flows", "Enable the user the capability to execute all read-only flows");
    }
}
